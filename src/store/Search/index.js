// 这是Search的小仓库

// state:仓库存储数据的地方
const state = {};
// mutations:修改state的唯一手段
const mutations = {};
// action：处理action,可以书写自己的业务逻辑，也可以处理异步
const action = {};
// getters: 理解为计算属性，可以简化仓库数据，让组件获得仓库的数据更加方便
const getters = {};

// 对外暴露
export default {
    state,
    mutations,
    action,
    getters
}